# One module name per line.
# Blank lines and lines starting with # are ignored.
#
# The script reads each module config from:
#   /opt/deploy/<module>/scripts/backup.conf
#
# Example:
# node
# project
