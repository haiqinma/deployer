# One module name per line.
# Blank lines and lines starting with # are ignored.
#
# The script searches backup files in /opt/backup by default:
#   <module>-YYYYMMDD-HHMMSS.sql.gz
# If multiple files exist for the same module, only the latest file is uploaded.
#
# Example:
# node
# app
