# release_notes.sh 可选配置
RELEASE_NOTES_CODEX_BIN='codex'

# 通知模版配置
# Type 可选值示例：compile package版本编译、product upgrade环境升级、release notes版本变更、monitor notify日志通知
# From 可选值示例：编译节点、router生产节点
NOTIFY_TYPE='变更说明'
NOTIFY_FROM='compile node'
